{
  "name": "fwc",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ folkengine/fwc",
    "create-local": "graph create --node http://localhost:8020/ folkengine/fwc",
    "remove-local": "graph remove --node http://localhost:8020/ folkengine/fwc",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 folkengine/fwc"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.9.0",
    "@graphprotocol/graph-ts": "0.9.0"
  }
}
