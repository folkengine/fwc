{
  "name": "fwc",
  "version": "1.0.0",
  "description": "Friendly Wagering Coin",
  "main": "index.js",
  "repository": "https://github.com/folkengine/fwc.git",
  "author": "Christoph <ignatz@gmail.com>",
  "license": "GPL-3.0",
  "scripts": {
    "compile": "cd contracts && yarn compile",
    "install": "npm-run-all --sequential install:*",
    "install:contracts": "cd contracts &&  yarn install",
    "install:client": "cd client &&  yarn install",
    "migrate": "cd contracts && yarn migrate",
    "test": "npm-run-all --sequential test:*",
    "test:client": "cd client && yarn test",
    "test:contracts": "cd contracts && yarn test",
    "watch": "cd client && react-scripts test"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
